# Preserve HOI4 mod files byte-for-byte.
# HOI4 is sensitive to encodings (localisation .yml must remain UTF-8 with BOM,
# many script files are Windows-1252) and to line endings. Disable git's EOL
# normalization and text re-encoding across the board so the bytes the game
# reads are exactly the bytes committed.
* -text
