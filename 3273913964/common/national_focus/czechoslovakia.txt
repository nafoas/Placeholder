focus_tree = {
	id = Czechoslovakia_Petka_Focus

	country = {
		factor= 0

		#modifier = {
		#	add = 10
		#	tag = CZE
		#}
	}
    default = no
    continuous_focus_position = { x = 0 y = 2000}

    focus = {
	    id = CZE_petka_start_focus
	    icon = GFX_focus_The_Petka
	
	    ai_will_do = { base = 100 }
	
	    x = 6
	    y = 0
	    cost = 5
	
	    available_if_capitulated = no
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_start_focus"
	    }
    }
    focus = {
	    id = CZE_petka_democracy
	    icon = GFX_focus_Give_Democracy
	
	    ai_will_do = { base = 100 }
	
	    x = 1
	    y = 1
	    cost = 5

	    prerequisite = {
		    focus = CZE_petka_start_focus
	    }
	
	    available_if_capitulated = no
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_democracy"
		    unlock_decision_tooltip = CZE_support_nat_dem_decision
		    add_to_variable = { 
			    var = CZE_national_democrat_influence
			    value = 1 
			}
		    subtract_from_variable = { 
		        var = CZE_agrarian_influence 
		        value = 1
		    }
	    }
    }
    focus = {
	    id = CZE_petka_fight_alcoholism
	    icon = GFX_focus_Ban_Alcohol
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_start_focus
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_fight_alcoholism"
	    }
    }
    focus = {
	    id = CZE_petka_unionists
	    icon = GFX_focus_Rose_of_Democracy
	
	    ai_will_do = { base = 100 }
	
	    x = 5
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_start_focus
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_unionists"
	    }
    }
    focus = {
	    id = CZE_petka_small_farmers
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 7
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_favour_christ_dem
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_small_farmers"
	    }
    }
    focus = {
	    id = CZE_petka_czech_identity
	    icon = GFX_focus_Czech_state
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_start_focus
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czechoslovak_identity
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_czech_identity"
	    }
    }
    focus = {
	    id = CZE_petka_czechoslovak_identity
	    icon = GFX_focus_Czechoslovak_state
	
	    ai_will_do = { base = 100 }
	
	    x = 11
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_start_focus
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czech_identity
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_czechoslovak_identity"
	    }
    }
    focus = {
	    id = CZE_petka_favour_craftsmen
	    icon = GFX_focus_Cooperatives
	
	    ai_will_do = { base = 100 }
	
	    x = 4
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_unionists
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_workers
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_favour_craftsmen"
	    }
    }
    focus = {
	    id = CZE_petka_favour_workers
	    icon = GFX_focus_Workers_Support
	
	    ai_will_do = { base = 100 }
	
	    x = 6
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_unionists
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_craftsmen
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_favour_workers"
	    }
    }
    focus = {
	    id = CZE_petka_catholic_values
	    icon = GFX_focus_For_Christ_and_Country
	
	    ai_will_do = { base = 100 }
	
	    x = 8
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_czech_identity
		    focus = CZE_petka_czechoslovak_identity
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_continue_secularisation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_catholic_values"
	    }
    }
    focus = {
	    id = CZE_petka_continue_secularisation
	    icon = GFX_focus_Church_State_Separate
	
	    ai_will_do = { base = 100 }
	
	    x = 12
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_czechoslovak_identity
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_catholic_values
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_continue_secularisation"
	    }
    }
    focus = {
	    id = CZE_petka_persecute_communists
	    icon = GFX_focus_Anti_Communist
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    available = {
			OR = {
			    AND = {
			        has_government = democratic
			        has_country_flag = national_democrats_are_soccon
			    }
			    has_government = jacobin
			}
			check_variable = {
				CZE_national_democrat_influence > 20
			}
			check_variable = {
				CZE_christian_democrat_influence > 20
			}
			check_variable = {
				CZE_traders_influence > 7
			}
		}

	    prerequisite = {
		    focus = CZE_petka_democracy
	    }
	    prerequisite = {
		    focus = CZE_petka_favour_craftsmen
	    }
	    prerequisite = {
		    focus = CZE_petka_fight_alcoholism
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_popular_front
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_agrarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_persecute_communists"
		    add_popularity = {
			    ideology = communism
			    popularity = -0.07
		    }
	    }
    }
    focus = {
	    id = CZE_petka_popular_front
	    icon = GFX_focus_Pride_of_The_Left
	
	    ai_will_do = { base = 100 }
	
	    x = 5
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_favour_workers
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_persecute_communists
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_christ_dem
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_agrarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_popular_front"
	    }
    }
    focus = {
	    id = CZE_petka_favour_christ_dem
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 7
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_catholic_values
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_popular_front
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_agrarians
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_hussites
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_favour_christ_dem"
	    }
    }
    focus = {
	    id = CZE_petka_favour_agrarians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 13
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_catholic_values
		    focus = CZE_petka_continue_secularisation
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_popular_front
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_persecute_communists
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_christ_dem
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_hussites
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_favour_agrarians"
	    }
    }
    focus = {
	    id = CZE_petka_hussites
	    icon = GFX_focus_Hussites
	
	    ai_will_do = { base = 100 }
	
	    x = 11
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_continue_secularisation
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_agrarians
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_favour_christ_dem
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_hussites"
	    }
    }
    focus = {
	    id = CZE_petka_national_bloc
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 1
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_persecute_communists
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_national_bloc"
	    }
    }
    focus = {
	    id = CZE_petka_pardon_socdems
	    icon = GFX_focus_Social_Democracy
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_persecute_communists
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_pardon_socdems"
	    }
    }
    focus = {
	    id = CZE_petka_yellow_bloc
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_favour_christ_dem
		    focus = CZE_petka_hussites
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_yellow_bloc"
	    }
    }
    focus = {
	    id = CZE_petka_revive_green_bloc
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 13
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_favour_agrarians
	    }
	    prerequisite = {
		    focus = CZE_petka_german_representation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_revive_green_bloc"
	    }
    }
    focus = {
	    id = CZE_petka_strenthen_trade_unions
	    icon = GFX_focus_Trade_Union_Focus
	
	    ai_will_do = { base = 100 }
	
	    x = 5
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_persecute_communists
		    focus = CZE_petka_popular_front
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_strenthen_trade_unions"
	    }
    }
    focus = {
	    id = CZE_petka_soviet_ties
	    icon = GFX_focus_Soviet_Relations
	
	    ai_will_do = { base = 100 }
	
	    x = 5
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_strenthen_trade_unions
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_soviet_ties"
	    }
    }
    focus = {
	    id = CZE_petka_appeal_to_workers
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 4
	    y = 6
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_soviet_ties
		    focus = CZE_petka_undermine_president
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_appeal_to_workers"
	    }
    }
    focus = {
	    id = CZE_petka_crush_natsocs
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 4
	    y = 8
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_appeal_to_workers
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_crush_natsocs"
	    }
    }
    focus = {
	    id = CZE_petka_invite_christ_dems
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 2
	    y = 8
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_fight_agrarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_invite_christ_dems"
	    }
    }
    focus = {
	    id = CZE_petka_legalise_kcs_militia
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 6
	    y = 6
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_soviet_ties
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_legalise_kcs_militia"
	    }
    }
    focus = {
	    id = CZE_petka_destroy_petka_from_within
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 5
	    y = 7
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_appeal_to_workers
	    }
	    prerequisite = {
		    focus = CZE_petka_legalise_kcs_militia
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_destroy_petka_from_within"
	    }
    }
    focus = {
	    id = CZE_petka_appeal_to_middle_class
	    icon = GFX_focus_Popular_Sentiment
	
	    ai_will_do = { base = 100 }
	
	    x = 11
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_yellow_bloc
		    focus = CZE_petka_strenthen_hrad
		    focus = CZE_petka_revive_green_bloc
		    focus = CZE_petka_small_farmers
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_appeal_to_middle_class"
	    }
    }
    focus = {
	    id = CZE_petka_agrarian_dominance
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 13
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_revive_green_bloc
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_agrarian_dominance"
	    }
    }
    focus = {
	    id = CZE_petka_appeal_to_catholics
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 7
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_small_farmers
		    focus = CZE_petka_yellow_bloc
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_appeal_to_catholics"
	    }
    }
    focus = {
	    id = CZE_petka_agrarian_reforms
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_yellow_bloc
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_agrarian_reforms"
	    }
    }
    focus = {
	    id = CZE_petka_strenthen_hrad
	    icon = GFX_focus_Supporters_of_the_president
	
	    ai_will_do = { base = 100 }
	
	    x = 11
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_hussites
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_strenthen_hrad"
	    }
    }
    focus = {
	    id = CZE_petka_undermine_president
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_national_bloc
	    }
	    prerequisite = {
		    focus = CZE_petka_pardon_socdems
	    }
	    prerequisite = {
		    focus = CZE_petka_strenthen_trade_unions
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_undermine_president"
	    }
    }
    focus = {
	    id = CZE_petka_fight_agrarians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 2
	    y = 6
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_undermine_president
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_fight_agrarians"
	    }
    }
    focus = {
	    id = CZE_petka_revive_hrad_party
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 6
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_appeal_to_middle_class
	    }
	    prerequisite = {
		    focus = CZE_petka_agrarian_reforms
	    }
	    prerequisite = {
		    focus = CZE_petka_appeal_to_catholics
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_revive_hrad_party"
	    }
    }
    focus = {
	    id = CZE_petka_undermine_natdems
	    icon = GFX_focus_Undermine_National_Democrats
	
	    ai_will_do = { base = 100 }
	
	    x = 7
	    y = 7
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_revive_hrad_party
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_undermine_natdems"
	    }
    }
    focus = {
	    id = CZE_petka_crush_agrarian_hegemony
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 7
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_revive_hrad_party
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_crush_agrarian_hegemony"
	    }
    }
    focus = {
	    id = CZE_petka_undermine_socdems
	    icon = GFX_Undermine_Social_Democrats
	
	    ai_will_do = { base = 100 }
	
	    x = 11
	    y = 7
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_revive_hrad_party
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_undermine_socdems"
	    }
    }
    focus = {
	    id = CZE_petka_groundwork_for_president
	    icon = GFX_focus_Presidential_System
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 8
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_undermine_natdems
	    }
	    prerequisite = {
		    focus = CZE_petka_undermine_socdems
	    }
	    prerequisite = {
		    focus = CZE_petka_crush_agrarian_hegemony
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_groundwork_for_president"
	    }
    }
    focus = {
	    id = CZE_petka_question_of_traders
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 7
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_appeal_to_workers
		    focus = CZE_petka_fight_agrarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_groundwork_for_president"
	    }
    }
    focus = {
	    id = CZE_petka_reform_constitution
	    icon = GFX_focus_German_Constitutionalism
	
	    ai_will_do = { base = 100 }
	
	    x = 3
	    y = 9
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_invite_christ_dems
	    }
	    prerequisite = {
		    focus = CZE_petka_crush_natsocs
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_reform_constitution"
	    }
    }
    focus = {
	    id = CZE_petka_new_constitution
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 9
	    y = 9
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_groundwork_for_president
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_new_constitution"
	    }
    }
    focus = {
	    id = CZE_petka_minorities_start
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 22
	    y = 0
	    cost = 5
	
	    available_if_capitulated = no
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_minorities_start"
	    }
    }
    focus = {
	    id = CZE_petka_help_sudeten_socdem
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 14
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_help_sudeten_depression
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_help_sudeten_socdem"
	    }
    }
    focus = {
	    id = CZE_petka_help_sudeten_depression
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 16
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_help_sudeten_depression"
		    remove_ideas = CZE_remains_of_great_depression
	    }
    }
    focus = {
	    id = CZE_petka_autonomisation
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 19
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czech_state
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czechoslovak_state
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_autonomisation"
	    }
    }
    focus = {
	    id = CZE_petka_czech_state
	    icon = GFX_focus_Czech_state
	
	    ai_will_do = { base = 100 }
	
	    x = 21
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_autonomisation
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czechoslovak_state
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_czech_state"
	    }
    }
    focus = {
	    id = CZE_petka_czechoslovak_state
	    icon = GFX_focus_Czechoslovak_state
	
	    ai_will_do = { base = 100 }
	
	    x = 23
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_czech_state
	    }
	    mutually_exclusive = {
		    focus = CZE_petka_autonomisation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_czechoslovak_state"
	    }
    }
    focus = {
	    id = CZE_petka_meet_hlinka
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 26
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_meet_hlinka"
	    }
    }
    focus = {
	    id = CZE_petka_status_rusyn_autonomy
	    icon = GFX_focus_Rusyn_Authonomy
	
	    ai_will_do = { base = 100 }
	
	    x = 28
	    y = 1
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_minorities_start
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_status_rusyn_autonomy"
	    }
    }
    focus = {
	    id = CZE_petka_reach_sudeten_agrarians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 16
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_help_sudeten_depression
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_reach_sudeten_agrarians"
	    }
    }
    focus = {
	    id = CZE_petka_weaken_autonomy
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 22
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_czech_state
		    focus = CZE_petka_czechoslovak_state
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_weaken_autonomy"
	    }
    }
    focus = {
	    id = CZE_petka_renegotiate_status_slovaks
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 24
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_meet_hlinka
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_renegotiate_status_slovaks"
	    }
    }
    focus = {
	    id = CZE_petka_renegotiate_status_hungarians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 26
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_meet_hlinka
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_renegotiate_status_hungarians"
	    }
    }
    focus = {
	    id = CZE_petka_reach_sudeten_christ_dem
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 18
	    y = 2
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_help_sudeten_depression
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_reach_sudeten_christ_dem"
	    }
    }
    focus = {
	    id = CZE_petka_strenthen_autonomy
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 19
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_autonomisation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_strenthen_autonomy"
	    }
    }
    focus = {
	    id = CZE_petka_remove_slovaks
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 21
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_czech_state
	    }
	    prerequisite = {
		    focus = CZE_petka_weaken_autonomy
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_remove_slovaks"
	    }
    }
    focus = {
	    id = CZE_petka_special_slovaks
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 23
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_czechoslovak_state
	    }
	    prerequisite = {
		    focus = CZE_petka_weaken_autonomy
	    }
	    prerequisite = {
		    focus = CZE_petka_renegotiate_status_slovaks
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_special_slovaks"
	    }
    }
    focus = {
	    id = CZE_petka_weaken_hungarians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 25
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_renegotiate_status_hungarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_weaken_hungarians"
	    }
    }
    focus = {
	    id = CZE_petka_separate_south_slovakia
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 27
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_renegotiate_status_hungarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_separate_south_slovakia"
	    }
    }
    focus = {
	    id = CZE_petka_rusyn_parliament
	    icon = GFX_focus_Rusyn_Parliament
	
	    ai_will_do = { base = 100 }
	
	    x = 29
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_status_rusyn_autonomy
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_rusyn_parliament"
	    }
    }
    focus = {
	    id = CZE_petka_german_representation
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 16
	    y = 3
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_reach_sudeten_christ_dem
		    focus = CZE_petka_reach_sudeten_agrarians
		    focus = CZE_petka_help_sudeten_socdem
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_german_representation"
	    }
    }
    focus = {
	    id = CZE_petka_rights_for_silesians
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 18
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_strenthen_autonomy
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_rights_for_silesians"
	    }
    }
    focus = {
	    id = CZE_petka_new_czechoslovak_state
	    icon = GFX_focus_Czechslovakia
	
	    ai_will_do = { base = 100 }
	
	    x = 24
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_special_slovaks
	    }
	    prerequisite = {
		    focus = CZE_petka_weaken_hungarians
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_new_czechoslovak_state"
	    }
    }
    focus = {
	    id = CZE_petka_slovak_parliament
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 26
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_weaken_hungarians
		    focus = CZE_petka_separate_south_slovakia
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_slovak_parliament"
	    }
    }
    focus = {
	    id = CZE_petka_hungarian_assembly
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 28
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_separate_south_slovakia
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_hungarian_assembly"
	    }
    }
    focus = {
	    id = CZE_petka_rusyn_government
	    icon = GFX_focus_Rusyn_Government
	
	    ai_will_do = { base = 100 }
	
	    x = 30
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_rusyn_parliament
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_rusyn_government"
	    }
    }
    focus = {
	    id = CZE_petka_henelein_question
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 15
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_german_representation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_henelein_question"
	    }
    }
    focus = {
	    id = CZE_petka_sudeten_assembly
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 16
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_german_representation
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_sudeten_assembly"
	    }
    }
    focus = {
	    id = CZE_petka_special_cieszyn
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 19
	    y = 5
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_rights_for_silesians
	    }
	    prerequisite = {
		    focus = CZE_petka_minority_rights
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_special_cieszyn"
	    }
    }
    focus = {
	    id = CZE_petka_minority_rights
	    icon = GFX_focus_WIP
	
	    ai_will_do = { base = 100 }
	
	    x = 20
	    y = 4
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_strenthen_autonomy
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_minority_rights"
	    }
    }
    focus = {
	    id = CZE_petka_zapadoslavia
	    icon = GFX_focus_Czechoslovak_Multicultural_state
	
	    ai_will_do = { base = 100 }
	
	    x = 24
	    y = 6
	    cost = 5
	
	    available_if_capitulated = no

	    prerequisite = {
		    focus = CZE_petka_sudeten_assembly
	    }
	    prerequisite = {
		    focus = CZE_petka_special_cieszyn
	    }
	    prerequisite = {
		    focus = CZE_petka_slovak_parliament
	    }
	    prerequisite = {
		    focus = CZE_petka_hungarian_assembly
	    }
	    prerequisite = {
		    focus = CZE_petka_rusyn_government
	    }
	
	    completion_reward = {
		    log = "[GetDateText]: [Root.GetName]: Focus CZE_petka_zapadoslavia"
	    }
    }
}