GEN_victim_plan = {
	name = "Allied Phoney War"
	desc = "Historical behavior for the Phoney War"

	enable = {
	    OR = {
		    original_tag = ENG
			original_tag = FRA
		}
		OR = {
			is_historical_focus_on = yes
			has_war_with = GER
		}
	}
	abort = {
	    OR = {
		    AND = {
			    tag = FRA
		        BEL = { has_capitulated = yes }
			    HOL = { has_capitulated = yes }
		    }
			AND = {
			    tag = ENG
		        FRA = { has_capitulated = yes }
		    }
		}
	}

	ai_national_focuses = {
	}

	focus_factors = {
	}
	research = {

	}

	ideas = {
		extensive_conscription = 0
		service_by_requirement = 0
		war_economy = 0
	}
	
	traits = {
	}

	ai_strategy = {
  		type = garrison
   		value = 60
	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}

}