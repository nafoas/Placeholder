##################################
# Occupation Decisions by Gaboemi
# Mantain in Alphapetical order
##################################

ocupation_decisions = {

	## Format ##
 #	tag_ocuppied = {
 #
 #		allowed = {
 #			NOT = {
 #				original_tag = tag
 #				original_tag = other_related_to_tag
 #				original_tag = civil_war_tag
 #			}
 #		}
 #
 #		activation = {
 #			CAPITAL_STATE_ID = {
 #				is_owned_and_controlled_by = ROOT
 #				is_claim_or_core_of_ROOT = no
 #			}
 #
 #			is_subject = no
 #			NOT = {
 #				has_country_flag = tag_annex
 #				country_exists = tag
 #			}
 #		}
 #
 #		# Always has to have this
 #		icon = generic_flags
 #		is_good = no
 #		selectable_mission = yes
 #		days_mission_timeout = 30
 #		cost = 0
 #		fire_only_once = no
 #
 #		cancel_trigger = {
 #			NOT = {
 #				CAPITAL_STATE_ID = { is_owned_and_controlled_by = ROOT }
 #			}
 #		}
 #
 #		complete_effect = {
 #			country_event = ocupation.n
 #		}
 #
 #
 #		timeout_effect = {
 #			country_event = ocupation.n
 #		}
 #		ai_will_do = {
 #			factor = 100
 #		}
 #	}
 	## End Format ##

 	# Afghanistan

 	AFG_occupied = {
		allowed = {
			NOT = {
				original_tag = AFG
			}
 		}

		activation = {
			267 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = AFG_annex
				country_exists = AFG
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				267 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.93
		}

		timeout_effect = {
			country_event = occupation.93
		}
		ai_will_do = {
			factor = 100
		}
	}

 	# Albania

 	ALB_occupied = {
		allowed = {
			NOT = {
				original_tag = ALB
				original_tag = ITA
				original_tag = RIT
			}
 		}

		activation = {
			if = {
				limit = {
					original_tag = GER
					GER = { has_war = yes }
				}
				always = no
			}
			44 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = ALB_annex
				country_exists = ALB
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				44 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.31
		}

		timeout_effect = {
			country_event = occupation.31
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Algeria

	ALG_occupied = {
		allowed = {
			NOT = {
				original_tag = ALG
				original_tag = FRA
			}
 		}

		activation = {
			459 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = ALG_annex
				country_exists = ALG
				OR = {
					capital_scope = {
						is_on_continent = europe
					}
					has_government = communism
					has_government = authoritarian_socialism
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				459 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.106
		}

		timeout_effect = {
			country_event = occupation.106
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Argentina

	ARG_occupied = {
		allowed = {
			NOT = {
				original_tag = ARG
			}
 		}

		activation = {
			278 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = ARG_annex
				country_exists = ARG
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				278 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.53
		}

		timeout_effect = {
			country_event = occupation.53
		}
		ai_will_do = {
			factor = 100
		}
	}

 	# Austria

	AUS_occupied = {
		allowed = {
			NOT = {
				original_tag = AUS
				original_tag = GER
			}
 		}

		activation = {
			4 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = AUS_annex
				country_exists = AUS
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				4 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.30
		}

		timeout_effect = {
			country_event = occupation.30
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Australia

	AST_occupied = {
		allowed = {
			NOT = {
				original_tag = AST
			}
 		}

		activation = {
			285 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = AST_annex
				country_exists = AST
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				285 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.59
		}

		timeout_effect = {
			country_event = occupation.59
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Bavaria - German Tag

	BAV_occupied = {
		allowed = {
			NOT = {
				original_tag = BAV
				original_tag = GER
				original_tag = DDR
				original_tag = WGR
			}
 		}

		activation = {
			52 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = BAV_annex
				country_exists = BAV
				64 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
				1176 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			OR = {
				NOT = {
					52 = { is_owned_and_controlled_by = ROOT }
				}
				64 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
				1176 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		complete_effect = {
			country_event = occupation.63
		}

		timeout_effect = {
			country_event = occupation.63
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Belgium

	BEL_occupied = {
		allowed = {
			NOT = {
				original_tag = BEL
			}
 		}

		activation = {
			6 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			34 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = BEL_annex
				OR = {
					has_country_flag = GER_hitler_in_power
					has_country_flag = GER_Military_Coup
					has_country_flag = GER_Himmler_Coup
				}
				country_exists = BEL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				6 = { is_owned_and_controlled_by = ROOT }
				34 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.8
		}

		timeout_effect = {
			country_event = occupation.8
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Bolivia

	BOL_occupied = {
		allowed = {
			NOT = {
				original_tag = BOL
			}
 		}

		activation = {
			302 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = BOL_annex
				country_exists = BOL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				302 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.39
		}

		timeout_effect = {
			country_event = occupation.39
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Bosnia

	BOS_occupied = {

		allowed = {
			NOT = {
				original_tag = BOS
				original_tag = YUG
				original_tag = GER
				original_tag = CRO
				original_tag = ITA
				original_tag = SER
			}
		}

		activation = {

			104 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			908 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			909 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = BOS_annex
				country_exists = BOS
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				104 = { is_owned_and_controlled_by = ROOT }
				908 = { is_owned_and_controlled_by = ROOT }
				909 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.47
		}

		timeout_effect = {
			country_event = occupation.47
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Bulgaria

	BUL_occupied = {
		allowed = {
			NOT = {
				original_tag = BUL
			}
 		}

		activation = {
			48 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = BUL_annex
				country_exists = BUL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				48 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.32
		}

		timeout_effect = {
			country_event = occupation.32
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Denmark

	DEN_occupied = {

		allowed = {
			NOT = {
				original_tag = DEN
			}
		}

		activation = {
			37 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			is_subject = no

			NOT = {
				has_country_flag = DEN_annex
				country_exists = DEN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				37 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.22
		}

		timeout_effect = {
			country_event = occupation.22
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Brazil

	BRA_occupied = {

		allowed = {
			NOT = {
				original_tag = BRA
			}
		}

		activation = {

			500 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = BRA_annex
				country_exists = BRA
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				500 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.41
		}

		timeout_effect = {
			country_event = occupation.41
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Britain

	ENG_occupied = {

		allowed = {
			NOT = {
				original_tag = ENG
				original_tag = UKP
				original_tag = SCO
				original_tag = WLS
				original_tag = EGL
			}
		}

		activation = {

			126 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			OR = {
				121 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
				122 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}

			is_subject = no

			NOT = {
				has_country_flag = ENG_annex
				country_exists = ENG
				country_exists = UKP
				country_exists = EGL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				13 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.35
		}

		timeout_effect = {
			country_event = occupation.35
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Canada

	CAN_occupied = {

		allowed = {
			NOT = {
				original_tag = CAN
				original_tag = QUE
			}
		}

		activation = {

			1005 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CAN_annex
				country_exists = CAN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				1005 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.2
		}

		timeout_effect = {
			country_event = occupation.2
		}
		ai_will_do = {
			factor = 100
		}
	}

	# California - American Tag

	CAL_occupied = {

		allowed = {
			NOT = {
				original_tag = CAL
				original_tag = USA
			}
		}

		activation = {

			378 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CAL_annex
				country_exists = CAL
				361 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			OR = {
				NOT = {
					378 = { is_owned_and_controlled_by = ROOT }
				}
				361 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		complete_effect = {
			country_event = occupation.72
		}

		timeout_effect = {
			country_event = occupation.72
		}
		ai_will_do = {
			factor = 100
		}
	}

	# China

	CHI_occupied = {

		allowed = {
			NOT = {
				original_tag = CHI
				original_tag = SHD
				original_tag = FUJ
				original_tag = PRC
				original_tag = GDC
				original_tag = HNA
				original_tag = GXC
				original_tag = GZC
				original_tag = YUN
				original_tag = LWH
				original_tag = DXH
				original_tag = TSY
				original_tag = LCH
				original_tag = YSN
				original_tag = LXI
				original_tag = XSM
				original_tag = MHK
				original_tag = YUL
				original_tag = SHX
				original_tag = NEA
				original_tag = MAN
				original_tag = JAP
			}
		}

		activation = {

			1107 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CHI_annex
			}
			OR = {
				AND = {
					NOT = { country_exists = CHI }
					NOT = {
						has_government = communism
						has_government = authoritarian_socialism
					}
				}
				AND = {
					NOT = { country_exists = PRC }
					OR = {
						has_government = communism
						has_government = authoritarian_socialism
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				1107 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.104
		}

		timeout_effect = {
			country_event = occupation.104
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Chile

	CHL_occupied = {

		allowed = {
			NOT = {
				original_tag = CHL
			}
		}

		activation = {

			279 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CHL_annex
				country_exists = CHL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				279 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.52
		}

		timeout_effect = {
			country_event = occupation.52
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Colombia

	COL_occupied = {

		allowed = {
			NOT = {
				original_tag = COL
			}
		}

		activation = {

			306 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = COL_annex
				country_exists = COL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				306 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.17
		}

		timeout_effect = {
			country_event = occupation.17
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Costa Rica

	COS_occupied = {

		allowed = {
			NOT = {
				original_tag = COS
			}
		}

		activation = {

			316 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = COS_annex
				country_exists = COS
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				316 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.9
		}

		timeout_effect = {
			country_event = occupation.9
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Croatia

	CRO_occupied = {

		allowed = {
			NOT = {
				original_tag = CRO
				original_tag = YUG
				original_tag = SER
				original_tag = GER
			}
		}

		activation = {

			109 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CRO_annex
				country_exists = CRO
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				109 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.48
		}

		timeout_effect = {
			country_event = occupation.48
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Cuba

	CUB_occupied = {

		allowed = {
			NOT = {
				original_tag = CUB
			}
		}

		activation = {

			315 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CUB_annex
				country_exists = CUB
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				315 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.56
		}

		timeout_effect = {
			country_event = occupation.56
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Czechoslovakia

	CZE_occupied = {

		allowed = {
			NOT = {
				original_tag = CZE
				original_tag = SLO
			}
		}

		activation = {

			9 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = CZE_annex
				country_exists = CZE
				AND = {
					original_tag = GER
					OR = {
						has_country_flag = GER_hitler_in_power
						has_country_flag = GER_Military_Coup
						has_country_flag = GER_Himmler_Coup
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				9 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.43
		}

		timeout_effect = {
			country_event = occupation.43
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Dominican Republic

	DOM_occupied = {

		allowed = {
			NOT = {
				original_tag = DOM
			}
		}

		activation = {

			319 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = DOM_annex
				country_exists = DOM
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				319 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.57
		}

		timeout_effect = {
			country_event = occupation.57
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Ecuador

	ECU_occupied = {

		allowed = {
			NOT = {
				original_tag = ECU
			}
		}

		activation = {

			305 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = ECU_annex
				country_exists = ECU
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				305 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.19
		}

		timeout_effect = {
			country_event = occupation.19
		}
		ai_will_do = {
			factor = 100
		}
	}

	#Eritrea

	ERI_occupied = {
 
		allowed = {
			NOT = {
 				original_tag = ETH
 				original_tag = ITA
 				original_tag = ENG

 			}
		}
 
		activation = {
			550 = {
				is_owned_and_controlled_by = ROOT
				is_claim_or_core_of_ROOT = no
			}
			NOT = {
				has_country_flag = ERI_annex
				AND = {
					owns_state = 271
					NOT = {
						has_country_flag = ETH_annex
					}
				}
			}
			is_subject = no
			NOT = {
				country_exists = ERI
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				550 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.102
		}


		timeout_effect = {
			country_event = occupation.102
		}
		ai_will_do = {
			factor = 100
		}
	}

	# El Salvador

	ELS_occupied = {

		allowed = {
			NOT = {
				original_tag = ELS
			}
		}

		activation = {

			314 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = ELS_annex
				country_exists = ELS
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				314 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.9
		}

		timeout_effect = {
			country_event = occupation.9
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Estonia

	EST_occupied = {

		allowed = {
			NOT = {
				original_tag = EST
				original_tag = SOV
			}
		}

		activation = {

			13 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = EST_annex
				country_exists = EST
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				13 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.26
		}

		timeout_effect = {
			country_event = occupation.26
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Ethiopia

	ETH_occupied = {
 
		allowed = {
			NOT = {
 				original_tag = ETH
 				original_tag = ITA
 			}
			NOT = {
				original_tag = ENG
			}
		}
 
		activation = {
			271 = {
				is_owned_and_controlled_by = ROOT
				is_claim_or_core_of_ROOT = no
			}
			NOT = {
				has_country_flag = ETH_annex
			}
			is_subject = no
			NOT = {
				country_exists = ETH
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				271 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.101
		}

		timeout_effect = {
			country_event = occupation.101
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Far East (FER)

	FER_occupied = {

		allowed = {
			NOT = {
				original_tag = SOV
				original_tag = RUS
				original_tag = RFP
				original_tag = FER
			}
		}
		
		visible = {
			NOT = {
				has_country_flag = is_in_AM_conflict
			}
		}

		activation = {

			408 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = FER_annex
				country_exists = FER
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				408 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.90
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.90
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Finland

	FIN_occupied = {

		allowed = {
			NOT = {
				original_tag = FIN
			}
		}

		activation = {

			111 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = FIN_annex
				country_exists = FIN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				111 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.24
		}

		timeout_effect = {
			country_event = occupation.24
		}
		ai_will_do = {
			factor = 100
		}
	}

	# France

	FRA_occupied = {
		allowed = {
			NOT = {
				original_tag = FRA
				original_tag = VIC
				original_tag = BRI
				original_tag = CRS
				original_tag = BUR
			}
 		}

		activation = {

			16 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = FRA_annex
				country_exists = FRA
				country_exists = VIC
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				16 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.1
		}

		timeout_effect = {
			country_event = occupation.1
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Germany
	GER_occupied = {

		allowed = {
			NOT = {
				original_tag = GER
				original_tag = DDR
				original_tag = WGR
				original_tag = BAV
				original_tag = RHN
				original_tag = PRS
			}
		}

		activation = {

			is_subject = no

			OR = {
				64 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}

				AND = {
					1176 = {
						is_owned_and_controlled_by = ROOT
						is_not_claim_or_core_of_ROOT = yes
					}
					OR = {
						52 = {
							OWNER = {
								OR = {
									tag = ROOT
									is_in_faction_with = ROOT
									is_puppet_of = ROOT
								}
							}
						}
						59 = {
							OWNER = {
								OR = {
									tag = ROOT
									is_in_faction_with = ROOT
									is_puppet_of = ROOT
								}
							}
						}
					}
				}

				AND = {
					59 = {
						is_owned_and_controlled_by = ROOT
						is_not_claim_or_core_of_ROOT = yes
					}
					57 = {
						OWNER = {
							OR = {
								tag = ROOT
								is_in_faction_with = ROOT
								is_puppet_of = ROOT
							}
						}
					}
					745 = {
						OWNER = {
							OR = {
								tag = ROOT
								is_in_faction_with = ROOT
								is_puppet_of = ROOT
							}
						}
					}
				}
			}

			if = {
				limit = {
					OR = {
						has_government = communism
						has_government = authoritarian_socialism
					}
				}
				NOT = { country_exists = DDR }
			}
			else_if = {
				limit = {
					OR = {
						has_government = fascism
						has_government = nationalism
						has_government = neutrality
						has_government = monarchism
					}
				}
				NOT = { country_exists = GER }
			}
			else = {
				NOT = { country_exists = WGR }
			}
			
			NOT = {
				has_country_flag = GER_annex
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				64 = { is_owned_and_controlled_by = ROOT }
				AND = {
					1176 = {
						is_owned_and_controlled_by = ROOT
						is_not_claim_or_core_of_ROOT = yes
					}
					OR = {
						52 = {
							OWNER = {
								OR = {
									tag = ROOT
									is_in_faction_with = ROOT
									is_puppet_of = ROOT
								}
							}
						}
						AND = {
							55 = {
								OWNER = {
									OR = {
										tag = ROOT
										is_in_faction_with = ROOT
										is_puppet_of = ROOT
									}
								}
							}
							57 = {
								OWNER = {
									OR = {
										tag = ROOT
										is_in_faction_with = ROOT
										is_puppet_of = ROOT
									}
								}
							}
						}
					}
				}

				AND = {
					59 = {
						is_owned_and_controlled_by = ROOT
						is_not_claim_or_core_of_ROOT = yes
					}
					57 = {
						OWNER = {
							OR = {
								tag = ROOT
								is_in_faction_with = ROOT
								is_puppet_of = ROOT
							}
						}
					}
					745 = {
						OWNER = {
							OR = {
								tag = ROOT
								is_in_faction_with = ROOT
								is_puppet_of = ROOT
							}
						}
					}
				}
			}
		}

		complete_effect = {
			country_event = occupation.62
		}

		timeout_effect = {
			country_event = occupation.62
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Greece

	GRE_occupied = {

		allowed = {
			NOT = {
				original_tag = GRE
			}
		}

		activation = {
			47 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			if = {
				limit = {
					OR = {
						AND = {
							original_tag = GER
							GER = { has_war = yes }
						}
						AND = {
							original_tag = ITA
							ITA = { has_war = yes }
						}
						AND = {
							original_tag = RIT
							RIT = { has_war = yes }
						}
					}
				}
				always = no
			}

			is_subject = no

			NOT = {
				has_country_flag = GRE_annex
				country_exists = GRE
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				47 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.33
		}

		timeout_effect = {
			country_event = occupation.33
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Guatemala

	GUA_occupied = {

		allowed = {
			NOT = {
				original_tag = GUA
			}
		}

		activation = {

			313 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = GUA_annex
				country_exists = GUA
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				313 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.4
		}

		timeout_effect = {
			country_event = occupation.4
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Haiti

	HAI_occupied = {

		allowed = {
			NOT = {
				original_tag = HAI
			}
		}

		activation = {

			318 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = HAI_annex
				country_exists = HAI
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				318 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.58
		}

		timeout_effect = {
			country_event = occupation.58
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Honduras

	HON_occupied = {

		allowed = {
			NOT = {
				original_tag = HON
			}
		}

		activation = {

			312 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = HON_annex
				country_exists = HON
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				312 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.5
		}

		timeout_effect = {
			country_event = occupation.5
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Hungary

	HUN_occupied = {

		allowed = {
			NOT = {
				original_tag = HUN
			}
		}

		activation = {

			any_owned_state = {
				is_core_of = HUN
				is_not_claim_or_core_of_ROOT = yes
				NOT = {
					state = 1046
					state = 1048
					state = 76
					state = 45
					state = 1031
					state = 664
					state = 1207
					state = 815
					state = 1054
				}
			}

			is_subject = no

			NOT = {
				has_country_flag = HUN_annex
				country_exists = HUN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				any_controlled_state = {
					is_core_of = HUN
					is_not_claim_or_core_of_ROOT = yes
				}
			}
		}

		complete_effect = {
			country_event = occupation.29
		}

		timeout_effect = {
			country_event = occupation.29
		}
		ai_will_do = {
			factor = 100
		}
	}

	# India

	RAJ_occupied = {

		allowed = {
			NOT = {
				original_tag = RAJ
				original_tag = HND
				original_tag = MYS
				original_tag = BRM
				original_tag = PAK
			}
		}

		activation = {

			439 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = RAJ_annex
				country_exists = RAJ
				country_exists = HND
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				439 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.55
		}

		timeout_effect = {
			country_event = occupation.55
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Iraq

	IRQ_occupied = {

		allowed = {
			NOT = {
				original_tag = IRQ
			}
		}

		activation = {

			291 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = IRQ_annex
				country_exists = IRQ
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				291 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.95
		}

		timeout_effect = {
			country_event = occupation.95
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Ireland

	IRE_occupied = {

		allowed = {
			NOT = {
				original_tag = IRE
				original_tag = ULS
				original_tag = ENG
			}
		}

		activation = {

			113 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = IRE_annex
				country_exists = IRE
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				113 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.20
		}

		timeout_effect = {
			country_event = occupation.20
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Italy

	ITA_occupied = {

		allowed = {
			NOT = {
				original_tag = ITA
				tag = RIT
			}
		}

		activation = {
			OR = {
				2 = { 
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
				159 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
				117 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}

			is_subject = no

			# if = {
			# 	limit = {
			# 		OR = {
			# 			has_government = fascism
			# 			has_government = nationalism
			# 		}
			# 	}
			# 	NOT = { country_exists = SRI }
			# }
			# else = {
			# 	NOT = { country_exists = ITA }
			# }

			NOT = {
				has_country_flag = ITA_annex
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				2 = { is_owned_and_controlled_by = ROOT }
				156 = { is_owned_and_controlled_by = ROOT }
				117 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.51
		}

		timeout_effect = {
			country_event = occupation.51
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Latvia

	LAT_occupied = {

		allowed = {
			NOT = {
				original_tag = LAT
				original_tag = SOV
			}
		}

		activation = {

			12 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = LAT_annex
				country_exists = LAT
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				12 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.27
		}

		timeout_effect = {
			country_event = occupation.27
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Liberia

	LIB_occupied = {
		allowed = {
			NOT = {
				original_tag = LIB
			}
 		}

		activation = {
			298 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = LIB_annex
				country_exists = LIB
				OR = {
					capital_scope = {
						is_on_continent = europe
					}
					has_government = communism
					has_government = authoritarian_socialism
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				298 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.53
		}

		timeout_effect = {
			country_event = occupation.53
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Lithuania

	LIT_occupied = {

		allowed = {
			NOT = {
				original_tag = LIT
				original_tag = SOV
			}
		}

		activation = {

			11 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = LIT_annex
				country_exists = LIT
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				11 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.28
		}

		timeout_effect = {
			country_event = occupation.28
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Luxemburg

	LUX_occupied = {

		allowed = {
			NOT = {
				original_tag = LUX
			}
		}

		activation = {

			8 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = LUX_annex
				OR = {
					has_country_flag = GER_hitler_in_power
					has_country_flag = GER_Military_Coup
					has_country_flag = GER_Himmler_Coup
				}
				country_exists = LUX
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				8 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.7
		}

		timeout_effect = {
			country_event = occupation.7
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Macedonia

	MCE_occupied = {

		allowed = {
			NOT = {
				original_tag = MCE
				original_tag = YUG
			}
		}

		activation = {

			916 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = MCE_annex
				country_exists = MCE
			}
			OR = {
				NOT = { 107 = { is_owned_and_controlled_by = ROOT } }
				original_tag = SER
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				916 = { is_owned_and_controlled_by = ROOT }
				OR = {
					NOT = { 107 = { is_owned_and_controlled_by = ROOT } }
					original_tag = SER
				}
			}
		}

		complete_effect = {
			country_event = occupation.50
		}

		timeout_effect = {
			country_event = occupation.50
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Mexico

	MEX_occupied = {

		allowed = {
			NOT = {
				original_tag = MEX
			}
		}

		activation = {

			277 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = MEX_annex
				country_exists = MEX
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				277 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.3
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.3
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Mongolia

	MON_occupied = {

		allowed = {
			NOT = {
				original_tag = MON
			}
		}

		activation = {
			OR = {
				NOT = { 
					original_tag = SOV
				}
				has_global_flag = Russian_Civil_War
			}
			330 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = MON_annex
				country_exists = MON
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				330 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.92
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.92
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Montenegro

	MNT_occupied = {

		allowed = {
			NOT = {
				original_tag = MNT
				original_tag = YUG
				original_tag = ITA
				tag = RIT
				original_tag = GER
				original_tag = SER
			}
		}

		activation = {

			OR = {
				105 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
				841 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}

			is_subject = no

			NOT = {
				has_country_flag = MNT_annex
				country_exists = MNT
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				105 = { is_owned_and_controlled_by = ROOT }
				OR = {
					NOT = { 107 = { is_owned_and_controlled_by = ROOT } }
					original_tag = SER
				}
			}
		}

		complete_effect = {
			country_event = occupation.46
		}

		timeout_effect = {
			country_event = occupation.46
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Netherlands / Holland

	HOL_occupied = {

		allowed = {
			NOT = {
				original_tag = LUX
			}
		}

		activation = {

			7 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = HOL_annex
				OR = {
					has_country_flag = GER_hitler_in_power
					has_country_flag = GER_Military_Coup
					has_country_flag = GER_Himmler_Coup
				}
				country_exists = HOL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				7 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.12
		}

		timeout_effect = {
			country_event = occupation.12
		}
		ai_will_do = {
			factor = 100
		}
	}

	# New Zealand

	NZL_occupied = {

		allowed = {
			NOT = {
				original_tag = NZL
			}
		}

		activation = {

			284 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = NZL_annex
				country_exists = NZL
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				284 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.60
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.60
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Nicaragua

	NIR_occupied = {

		allowed = {
			NOT = {
				original_tag = NIC
			}
		}

		activation = {

			317 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = NIC_annex
				country_exists = NIC
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				317 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.10
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.10
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Norway

	NOR_occupied = {

		allowed = {
			NOT = {
				original_tag = NOR
			}
		}

		activation = {

			110 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				OR = {
					has_country_flag = GER_hitler_in_power
					has_country_flag = GER_Military_Coup
					has_country_flag = GER_Himmler_Coup
				}
				has_country_flag = NOR_annex
				country_exists = NOR
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				110 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.25
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.25
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Polonia

	POL_occupied = {

		allowed = {
			NOT = {
				original_tag = POL
			}
		}

		activation = {

			10 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = POL_annex
				country_exists = POL
				AND = {
					original_tag = GER
					OR = {
						has_country_flag = GER_hitler_in_power
						has_country_flag = GER_Military_Coup
						has_country_flag = GER_Himmler_Coup
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				10 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.42
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.42
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Panama

	PAN_occupied = {

		allowed = {
			NOT = {
				original_tag = PAN
			}
		}

		activation = {

			304 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = PAN_annex
				country_exists = PAN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				304 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.16
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.16
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Paraguay

	PAR_occupied = {

		allowed = {
			NOT = {
				original_tag = PAR
			}
		}

		activation = {

			871 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = PAR_annex
				country_exists = PAR
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				871 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.100
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.100
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Peru

	PRU_occupied = {

		allowed = {
			NOT = {
				original_tag = PRU
			}
		}

		activation = {

			303 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = PRU_annex
				country_exists = PRU
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				303 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.21
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.21
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Persia
 	PER_occupied = {
		allowed = {
			NOT = {
				original_tag = PER
			}
 		}

		activation = {

			266 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = PER_annex
				country_exists = PER
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				266 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.94
		}

		timeout_effect = {
			country_event = occupation.94
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Philippines

 	PHI_occupied = {
		allowed = {
			NOT = {
				original_tag = PHI
			}
 		}

		activation = {

			327 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = PHI_annex
				country_exists = PHI
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				327 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.99
		}

		timeout_effect = {
			country_event = occupation.99
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Portugal

	POR_occupied = {

		allowed = {
			NOT = {
				original_tag = POR
			}
		}

		activation = {

			112 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = POR_annex
				country_exists = POR
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				112 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.15
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.15
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Rhineland - German Tag

	RHN_occupied = {
		allowed = {
			NOT = {
				original_tag = RHN
				original_tag = GER
				original_tag = DDR
				original_tag = WGR
			}
 		}

		activation = {

			1176 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = RHN_annex
				country_exists = RHN
				64 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
				52 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
				59 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			OR = {
				NOT = {
					1176 = { is_owned_and_controlled_by = ROOT }
				}
				64 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
				52 = {
					OWNER = {
						OR = {
							tag = ROOT
							is_in_faction_with = ROOT
							is_puppet_of = ROOT
						}
					}
				}
			}
		}

		complete_effect = {
			country_event = occupation.65
		}

		timeout_effect = {
			country_event = occupation.65
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Romania

	ROM_occupied = {

		allowed = {
			NOT = {
				original_tag = ROM
			}
		}

		activation = {

			46 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = ROM_annex
				country_exists = ROM
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				46 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.38
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.38
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Russia / Soviet Union

	RUS_occupied = {

		allowed = {
			NOT = {
				original_tag = SOV
				original_tag = RUS
				original_tag = RFP
			}
		}

		activation = {

			219 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = RUS_annex
				country_exists = RUS
				country_exists = SOV
				country_exists = RFP
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				219 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.75
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.75
		}
		ai_will_do = {
			factor = 100
		}
	}
	
	# Saudi Arabia

	SAU_occupied = {

		allowed = {
			NOT = {
				original_tag = SAU
			}
		}

		activation = {

			292 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SAU_annex
				country_exists = SAU
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				292 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.96
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.96
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Scotland

	SCO_occupied = {

		allowed = {
			NOT = {
				original_tag = SCO
				original_tag = ENG
			}
		}

		activation = {

			121 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SCO_annex
				has_country_flag = ENG_annex
				country_exists = SCO
				country_exists = ENG
				126 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				126 = { is_owned_and_controlled_by = ROOT }
			}
			121 = { is_owned_and_controlled_by = ROOT }
		}

		complete_effect = {
			country_event = occupation.36
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.36
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Serbia

	SER_occupied = {

		allowed = {
			NOT = {
				original_tag = SER
				original_tag = YUG
			}
		}

		activation = {

			107 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SER_annex
				country_exists = SER
			}
			OR = {
				NOT = { 104 = { is_owned_and_controlled_by = ROOT } }
				country_exists = BOS
			}
			OR = {
				NOT = { 109 = { is_owned_and_controlled_by = ROOT } }
				country_exists = CRO
			}
			OR = {
				NOT = { 105 = { is_owned_and_controlled_by = ROOT } }
				country_exists = MNT
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			OR = {
				NOT = {
					107 = { is_owned_and_controlled_by = ROOT }
				}
				AND = {
					104 = { is_owned_and_controlled_by = ROOT }
					109 = { is_owned_and_controlled_by = ROOT }
					105 = { is_owned_and_controlled_by = ROOT }
				}
			}
		}

		complete_effect = {
			country_event = occupation.49
		}

		timeout_effect = {
			country_event = occupation.49
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Slovakia

	SLO_occupied = {

		allowed = {
			NOT = {
				original_tag = SLO
				original_tag = CZE
			}
		}

		activation = {

			70 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SLO_annex
				country_exists = SLO
			}

			9 = {
				OR = {
					NOT = { is_owned_and_controlled_by = ROOT }
					OWNER = {
						OR = {
							has_country_flag = CZE_annex
							AND = {
								original_tag = GER
								OR = {
									has_country_flag = GER_hitler_in_power
									has_country_flag = GER_Military_Coup
									has_country_flag = GER_Himmler_Coup
								}
							}
						}
					}
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				70 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.44
		}

		timeout_effect = {
			country_event = occupation.44
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Slovenia

	SLV_occupied = {

		allowed = {
			NOT = {
				original_tag = SLV
				original_tag = YUG
				original_tag = GER
				original_tag = ITA
				tag = RIT
				original_tag = AUS
			}
		}

		activation = {

			OR = {
				102 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
				838 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}

			is_subject = no

			NOT = {
				has_country_flag = SLV_annex
				country_exists = SLV
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				838 = { is_owned_and_controlled_by = ROOT }
				OR = {
					NOT = { 107 = { is_owned_and_controlled_by = ROOT } }
					original_tag = SER
				}
			}
		}

		complete_effect = {
			country_event = occupation.49
		}

		timeout_effect = {
			country_event = occupation.49
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Somalia

	SMA_occupied = {
 
		allowed = {
			NOT = {
 				original_tag = ENG
 				original_tag = ITA

 			}
		}
 
		activation = {
			559 = {
				is_owned_and_controlled_by = ROOT
				is_claim_or_core_of_ROOT = no
			}
			NOT = {
				has_country_flag = SMA_annex
			}
			is_subject = no
			NOT = {
				country_exists = SMA
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				559 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.103
		}


		timeout_effect = {
			country_event = occupation.103
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Siam

	SIA_occupied = {

		allowed = {
			NOT = {
				original_tag = SIA
			}
		}

		activation = {

			289 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SIA_annex
				country_exists = SIA
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				289 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.98
		}

		timeout_effect = {
			country_event = occupation.98
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Spain

	SPR_occupied = {

		allowed = {
			NOT = {
				original_tag = SPR
				original_tag = SPA
				original_tag = SPC
			}
		}

		activation = {

			41 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SPR_annex
				country_exists = SPR
				country_exists = SPA
				country_exists = SPC
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				41 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			if = {
				limit = {
					OR = {
						has_government = fascism
						has_government = nationalism
						has_government = monarchism
						has_government = neutrality
					}				
				}
				country_event = occupation.13
			}
			else_if = {
				limit = {
					OR = {
						has_government = communism
						has_government = authoritarian_socialism
						has_government = socialism
						has_government = social_democracy
						has_government = social_liberalism
						has_government = market_liberalism
						has_government = jacobin
						has_government = democratic
					}
				}
				country_event = occupation.14
			}
		}

		# Always have this
		timeout_effect = {
			if = {
				limit = {
					OR = {
						has_government = fascism
						has_government = nationalism
						has_government = monarchism
						has_government = neutrality
					}				
				}
				country_event = occupation.13
			}
			else_if = {
				limit = {
					OR = {
						has_government = communism
						has_government = authoritarian_socialism
						has_government = socialism
						has_government = social_democracy
						has_government = social_liberalism
						has_government = market_liberalism
						has_government = jacobin
						has_government = democratic
					}
				}
				country_event = occupation.14
			}
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Sweden

	SWE_occupied = {

		allowed = {
			NOT = {
				original_tag = SWE
			}
		}

		activation = {

			141 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SWE_annex
				country_exists = SWE
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				141 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.23
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.23
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Switzerland

	SWI_occupied = {

		allowed = {
			NOT = {
				original_tag = SWI
			}
		}

		activation = {

			3 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = SWI_annex
				country_exists = SWI
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				3 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.6
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.6
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Tannu Tuva
	TAN_occupied = {

		allowed = {
			NOT = {
				original_tag = TAN
			}
		}

		activation = {
			OR = {
				NOT = {
					original_tag = SOV
				}
				has_global_flag = Russian_Civil_War
			}
			329 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = TAN_annex
				country_exists = TAN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				329 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.97
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.97
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Texas - American Tag

#	TEX_occupied = {
#
#		allowed = {
#			NOT = {
#				original_tag = TEX
#				original_tag = USA
#			}
#		}
#
#		activation = {
#
#			378 = {
#				is_owned_and_controlled_by = ROOT
#				is_not_claim_or_core_of_ROOT = yes
#			}
#
#			is_subject = no
#
#			NOT = {
#				has_country_flag = TEX_annex
#				country_exists = TEX
#				361 = {
#					OWNER = {
#						OR = {
#							tag = ROOT
#							is_in_faction_with = ROOT
#							is_puppet_of = ROOT
#						}
#					}
#				}
#			}
#		}
#
#		# Always has to have this
#		icon = generic_flags
#		is_good = no
#		selectable_mission = yes
#		days_mission_timeout = 30
#		cost = 0
#		fire_only_once = no
#
#		cancel_trigger = {
#			OR = {
#				NOT = {
#					378 = { is_owned_and_controlled_by = ROOT }
#				}
#				361 = {
#					OWNER = {
#						OR = {
#							tag = ROOT
#							is_in_faction_with = ROOT
#							is_puppet_of = ROOT
#						}
#					}
#				}
#			}
#		}
#
#		complete_effect = {
#			country_event = occupation.70
#		}
#
#		timeout_effect = {
#			country_event = occupation.70
#		}
#		ai_will_do = {
#			factor = 100
#		}
#	}

	# Turkey

	TUR_occupied = {

		allowed = {
			NOT = {
				original_tag = TUR
			}
		}

		activation = {

			49 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = TUR_annex
				country_exists = TUR
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				49 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.34
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.34
		}
		ai_will_do = {
			factor = 100
		}
	}

	# United States of America

	USA_occupied = {

		allowed = {
			NOT = {
				original_tag = USA
			}
		}

		activation = {

			361 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = USA_annex
				country_exists = USA
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				361 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.66
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.66
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Uruguay

	URG_occupied = {

		allowed = {
			NOT = {
				original_tag = URG
			}
		}

		activation = {

			300 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = URG_annex
				country_exists = URG
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				300 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.54
		}

		timeout_effect = {
			country_event = occupation.54
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Venezuela

	VEN_occupied = {

		allowed = {
			NOT = {
				original_tag = VEN
			}
		}

		activation = {

			307 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = VEN_annex
				country_exists = VEN
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				307 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.18
		}

		timeout_effect = {
			country_event = occupation.18
		}
		ai_will_do = {
			factor = 100
		}
	}


	# Wales

	WLS_occupied = {

		allowed = {
			NOT = {
				original_tag = WLS
				original_tag = ENG
			}
		}

		activation = {

			122 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no

			NOT = {
				has_country_flag = WLS_annex
				has_country_flag = ENG_annex
				country_exists = WLS
				country_exists = ENG
				126 = {
					is_owned_and_controlled_by = ROOT
					is_not_claim_or_core_of_ROOT = yes
				}
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				122 = { is_owned_and_controlled_by = ROOT }
			}
			126 = { is_owned_and_controlled_by = ROOT }
		}

		complete_effect = {
			country_event = occupation.37
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.37
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Xinjiang

	SIK_occupied = {
		allowed = {
			NOT = {
				original_tag = SIK
			}
 		}

		activation = {
			617 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}

			is_subject = no
			NOT = {
				has_country_flag = SIK_annex
				country_exists = SIK
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
 		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				617 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.61
		}

		timeout_effect = {
			country_event = occupation.61
		}
		ai_will_do = {
			factor = 100
		}
	}

	# Yugoslavia

	YUG_occupied = {

		allowed = {
			NOT = {
				original_tag = GER
				original_tag = ITA
				original_tag = YUG
				original_tag = SER
				original_tag = BOS
				original_tag = CRO
				original_tag = SLO
				original_tag = SLV
				original_tag = MCE
				original_tag = MNT
			}
		}

		activation = {

			107 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			104 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			109 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			105 = {
				is_owned_and_controlled_by = ROOT
				is_not_claim_or_core_of_ROOT = yes
			}
			
			is_subject = no

			NOT = {
				has_country_flag = YUG_annex
				country_exists = YUG
				country_exists = CRO
				country_exists = SER
				country_exists = BOS
			}
		}

		# Always has to have this
		icon = generic_flags
		is_good = no
		selectable_mission = yes
		days_mission_timeout = 30
		cost = 0
		fire_only_once = no

		cancel_trigger = {
			NOT = {
				107 = { is_owned_and_controlled_by = ROOT }
				104 = { is_owned_and_controlled_by = ROOT }
				109 = { is_owned_and_controlled_by = ROOT }
				105 = { is_owned_and_controlled_by = ROOT }
			}
		}

		complete_effect = {
			country_event = occupation.45
		}

		# Always have this
		timeout_effect = {
			country_event = occupation.45
		}
		ai_will_do = {
			factor = 100
		}
	}

	# return_tag_lands = {
	# 	icon = infiltrate_state
	# 	available = {
	# 		OR = {
	# 			has_war = no
	# 			surrender_progress < 0.10
	# 		}
	# 	}

	# 	visible = {
	# 		NOT = { has_country_flag = already_returning_clay }
	# 		any_owned_state = {
	# 			NOT = {
	# 				has_state_flag = keeping_clay@ROOT
	# 			}
	# 			is_claim_or_core_of_FROM = yes
	# 			is_not_claim_or_core_of_ROOT = yes
	# 			is_controlled_by = ROOT
	# 			OR = {
	# 				any_neighbor_state = { is_owned_by = FROM }
	# 				AND = {
	# 					is_coastal = yes
	# 					FROM = {
	# 						any_owned_state  = {
	# 							is_coastal = yes
	# 						}
	# 					}
	# 				}
	# 			}
	# 		}
	# 	}
		
	# 	target_root_trigger = {
	# 		OR = {
	# 			is_in_faction = yes
	# 			num_subjects > 0
	# 		}
	# 		is_subject = no
	# 	}

	# 	target_trigger = {
	# 		FROM = {
	# 			OR = {
	# 				AND = {
	# 					NOT = { is_subject_of = ROOT }
	# 					is_in_faction_with = ROOT
	# 				}
	# 				AND = {
	# 					is_subject_of = ROOT
	# 					capital_scope = {
	# 						NOT = {
	# 							is_on_continent = africa
	# 						}
	# 					}
	# 				}
	# 			}
	# 		}
	# 	}

	# 	target_array = allies
	# 	target_array = subjects

	# 	priority = 1

	# 	complete_effect = {
	# 		FROM = { save_event_target_as = returning_tag }
	# 		set_country_flag = already_returning_clay
	# 		country_event = occupation.1000
	# 	}

	# 	ai_will_do = {
	# 		factor = 100
	# 	}
	# }
}