on_actions = {
	# country 
	on_new_term_election = {
		effect = {
			if = {
				limit = { tag = CRI }
				country_event = { id = cri_internal.4 } #replace with cri_internal.4 and 5
			}
		}
	}
}
