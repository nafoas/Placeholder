ROM_highway_to_the_danger_zone_78 = { # bessarabia
	enable = {
		tag = ROM
		has_war = no
		date > 1938.1.1
		78 = {
			is_controlled_by = ROM
			infrastructure < 8
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = infrastructure
		target = 78
		value = 1
	}	
}

ROM_highway_to_the_danger_zone_1052 = { # s. bessarabia
	enable = {
		tag = ROM
		has_war = no
		date > 1938.1.1
		78 = {
			OR = {
				NOT = { is_controlled_by = ROM }
				infrastructure > 7
			}
		}
		766 = {
			is_controlled_by = ROM
			infrastructure < 8
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = infrastructure
		target = 766
		value = 1
	}	
}

ROM_highway_to_the_danger_zone_1051 = { # Moldova
	enable = {
		tag = ROM
		has_war = no
		date > 1938.1.1
		78 = {
			OR = {
				NOT = { is_controlled_by = ROM }
				infrastructure > 7
			}
		}
		766 = {
			OR = {
				NOT = { is_controlled_by = ROM }
				infrastructure > 7
			}
		}
		79 = {
			is_controlled_by = ROM
			infrastructure < 8
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = build_building
		id = infrastructure
		target = 79
		value = 1
	}	
}