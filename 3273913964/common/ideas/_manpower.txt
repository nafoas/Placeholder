ideas = {

	mobilization_laws = { 
		law = yes
		use_list_view = yes

		disarmed_nation = {
			cost = 150
			removal_cost = -1
			level = 7

			available = {
				#has_manpower_for_recruit_change_to = { value = 0.005 group = mobilization_laws }
			}

			modifier = {
				conscription = 0.005
			}

			allowed_to_remove = {
				# NOT = {
				# 	has_idea = HUN_treaty_of_trianon
				# }
				NOT = {
					tag = DZG
					tag = PHI
					tag = GRN
					tag = ICE
					tag = TGR
					tag = JOR
					tag = LEB
					tag = SYR
				}
			}

			cancel_if_invalid = no
		}

		volunteer_only = {
			cost = 150
			removal_cost = -1
			level = 6

			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.015 group = mobilization_laws }
				NOT = { has_idea = undisturbed_isolation }
				NOT = { has_idea = isolation }
				if = {
					limit = {
						OR = {
							original_tag = GER
							original_tag = WGR
							original_tag = DDR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}
			}

			modifier = {
				conscription = 0.01
			}

			ai_will_do = {
				base = 2
				modifier = {
					tag = PHI
					factor = 0
				}
			}

			allowed_to_remove = {
				if = {
					limit = { tag = PHI }
					is_subject = no
				}
			}

			default = yes
			cancel_if_invalid = no
			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted volunteer_only law"
			}
		}

		limited_conscription = {
			cost = 150
			removal_cost = -1
			level = 5

			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted limited_conscription law"
			}

			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.025 group = mobilization_laws }
				NOT = {
					has_idea = undisturbed_isolation
					has_idea = isolation
				}
				if = {
					limit = {
						original_tag = RAJ
						has_dlc = "Together for Victory"
					}
					is_subject = no
				}
				if = {
					limit = {
						OR = {
							original_tag = GER
							original_tag = DDR
							original_tag = WGR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}
				if = {
					limit = {
						tag = ENG
					}
					OR = {
						has_war = yes
						has_completed_focus = ENG_Rearm_Conscription
						date > 1940.1.1
					}
				}
				if = {
					limit = {
						NOT = {
							tag = ENG
							tag = RAJ
						}
					}
					OR = {
						AND = {
							if = {
								limit = {
									not_has_democratic_government = yes
									has_war = no
								}
								custom_trigger_tooltip = {
									tooltip = party_pop_greater_than_10_tt
									check_variable = {total_party_pop_dec > 0.1}
								}
							}
							else_if = {
								limit = {
									has_democratic_government = yes
									has_war = no
								}
								custom_trigger_tooltip = {
									tooltip = party_pop_greater_than_50_tt
									check_variable = {total_party_pop_dec > 0.5}
								}
							}
							has_war_support > 0.25
						}
						has_war = yes
					}
				}
			}

			modifier = {
				conscription = 0.025
			}

			ai_will_do = {
				base = 2
				modifier = {
					#Prio if low on manpower
					has_idea = volunteer_only
					add = 500
					manpower_per_military_factory <  1000
				}
				modifier = {
					has_war = no
					is_major = no
					factor = 0.2
				}
				modifier = {
					has_war = no
					has_democratic_government = yes
					
					factor = 0.3
				}
				modifier = {
					OR = {
						tag = DEN
						tag = LUX
						tag = LIB
						tag = TIB
						tag = COL
					}
					has_war = no
					
					factor = 0
				}
				modifier = {
					has_idea = volunteer_only
					has_war = yes
					add = 500
				}
				modifier = {
					has_manpower > 0
					enemies_strength_ratio < 0.5
					has_war = yes
					factor = 0
				}
			}

			cancel_if_invalid = no
		}

		extensive_conscription = {
			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted extensive_conscription law"
				if = {
					limit = {
						has_war_support < 0.5
					}
					add_political_power = -30
					add_stability = -0.02
				}
			}
			available = {
				#has_manpower_for_recruit_change_to = { value = 0.05 group = mobilization_laws }
				OR = {
					has_idea = limited_conscription
					has_idea = service_by_requirement
					has_idea = all_adults_serve
					has_idea = scraping_the_barrel
				}
				if = {
					limit = {
						OR = {
							tag = GER
							tag = DDR
							tag = WGR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}

				if = {
					limit = {
						OR = {
							tag = ENG
							tag = FRA
						}
						has_democratic_government = yes
					}
					OR = {
						AND = {
							custom_trigger_tooltip = {
								tooltip = party_pop_greater_than_60_tt
								check_variable = {total_party_pop_dec > 0.55}
							}
							has_war_support > 0.6
						}
						has_war_support > 0.8
						AND = {
							has_war = yes
							has_war_support > 0.35
							OR = {
								enemies_strength_ratio > 0.5
								has_manpower < 1000
								surrender_progress > 0.15
							}
						}
					}
				}
				else = {
					OR = {
						if = {
							limit = {
								not_has_democratic_government = yes
								has_war = no
							}
							OR = {
								AND = {
									custom_trigger_tooltip = {
										tooltip = party_pop_greater_than_30_tt
										check_variable = {total_party_pop_dec > 0.3}
									}
									has_war_support > 0.4
								}
								has_war_support > 0.55
							}
						}
						else_if = {
							limit = {
								has_democratic_government = yes
								has_war = no
							}
							OR = {
								AND = {
									custom_trigger_tooltip = {
										tooltip = party_pop_greater_than_50_tt
										check_variable = {total_party_pop_dec > 0.5}
									}
									has_war_support > 0.45
								}
								has_war_support > 0.65
							}
						}
						AND = {
							has_war = yes
							has_war_support > 0.35
							OR = {
								enemies_strength_ratio > 0.5
								has_manpower < 1000
								surrender_progress > 0.15
							}
						}
					}
				}

			}

			cost = 150
			removal_cost = -1
			level = 4

			modifier = {
				conscription = 0.05
				#industrial_capacity_factory = -0.1
				#production_speed_buildings_factor = -0.1
				training_time_factor = 0.1
			}

			ai_will_do = {
				base = 2
				modifier = {
					has_war_support > 0.8
					add = 100
				}
				modifier = {
					has_war = yes
					add = 300
				}
				modifier = {
					not_has_democratic_government = yes
					is_major = yes
					add = 20
				}
				modifier = {
					is_major = no
					has_war = no
					NOT = {
						check_variable = { ai_attitude_is_threatened = 1 }
					}
					factor = 0
				}
				modifier = {
					is_subject = yes
					OR = {
						is_major = no
						NOT = {
							tag = CAN
							tag = AST
							tag = SAF
							tag = RAJ
							tag = NZL
							tag = IRE
						}
					}
					has_war = no
					factor = 0
				}
				modifier = {
					OR = {
						tag = DEN
						tag = NOR
						tag = LUX
						tag = LIB
						tag = TIB
						tag = BHU
						tag = NEP
						tag = DOM
						tag = HAI
						tag = GUA
						tag = HON
						tag = ELS
						tag = COS
						tag = PAN
						tag = OMA
					}
					has_war = no
					
					factor = 0
				}
				modifier = {
					#Prio if low on manpower
					add = 600
					manpower_per_military_factory <  800
				}
				modifier = {
					factor = 0
					has_war = yes
					enemies_strength_ratio < 0.5
					has_manpower > 0
				}

				# modifier = { # china relies a lot on throwing bodies at the enemy
				# 	factor = 3

				# 	tag = CHI
				# 	has_manpower < 200000
				# }
			}
			cancel_if_invalid = no
		}

		service_by_requirement = {
			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted service_by_requirement law"
				if = {
					limit = {
						has_war_support < 0.5
					}
					add_political_power = -50
					add_stability = -0.05
				}
				else_if = {
					limit = {
						has_war_support < 0.6
					}
					add_political_power = -15
					add_stability = -0.01
				}
			}
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.1 group = mobilization_laws }
				OR = {
					if = {
						limit = {
							not_has_democratic_government = yes
							has_war = no
						}
						OR = {
							AND = {
								custom_trigger_tooltip = {
									tooltip = party_pop_greater_than_30_tt
									check_variable = {total_party_pop_dec > 0.3}
								}
								has_war_support > 0.45
							}
							has_war_support > 0.6
						}
					}
					if = {
						limit = {
							has_democratic_government = yes
							has_war = no
						}
						OR = {
							AND = {
								custom_trigger_tooltip = {
									tooltip = party_pop_greater_than_40_tt
									check_variable = {total_party_pop_dec > 0.4}
								}
								has_war_support > 0.6
							}
							has_war_support > 0.8
						}
					}
					AND = {
						has_war = yes
						OR = {
							enemies_strength_ratio > 0.6
							has_manpower < 1000
							surrender_progress > 0
						}
					}
				}
				OR = {
					has_idea = extensive_conscription
					has_idea = all_adults_serve
					has_idea = scraping_the_barrel
				}
				if = {
					limit = {
						OR = {
							tag = GER
							tag = DDR
							tag = WGR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}
			}

			ai_will_do = {
				base = 2
				modifier = {
					#Prio if low on manpower
					add = 700
					manpower_per_military_factory <  700
				}
				modifier = {
					#Lower chance if has decent amount
					factor = 0.5
					manpower_per_military_factory >  1000
				}
				modifier = {
					#Block has plenty manpower
					factor = 0
					NOT = { tag = CHI }
					NOT = { tag = SOV }
					manpower_per_military_factory >  3000
				}
				modifier = {
					is_subject = yes
					OR = {
						is_major = no
						NOT = {
							tag = CAN
							tag = AST
							tag = SAF
							tag = RAJ
							tag = NZL
							tag = IRE
						}
					}
					has_war = no
					factor = 0
				}

				modifier = {
					OR = {
						tag = DEN
						tag = NOR
						tag = LUX
						tag = LIB
						tag = TIB
						tag = BHU
						tag = NEP
						tag = GUA
						tag = DOM
						tag = HAI
						tag = HON
						tag = ELS
						tag = COS
						tag = PAN
						tag = OMA
					}
					has_war = no
					
					factor = 0
				}

				modifier = {
					has_war = yes
					add = 150
				}

				modifier = { # china relies a lot on throwing bodies at the enemy
					factor = 3

					tag = CHI
					has_war = yes
					has_manpower < 200000
				}

				modifier = { # germany needs this before russia, badly
					factor = 10

					OR = {
						tag = GER
						tag = DDR
					}
					has_war = yes
					has_manpower < 2000000
				}
				modifier = {
					factor = 500
					original_tag = SOV
					SOV = { has_war = yes }
					check_variable = { manpower_k < 3000 }
				}
				modifier = {
					factor = 0
					has_war = yes
					enemies_strength_ratio < 0.5
					has_manpower > 0
				}
			}

			cost = 150
			removal_cost = -1
			level = 3

			modifier = {
				conscription = 0.1
				industrial_capacity_factory = -0.1
				industrial_capacity_dockyard = -0.1
				production_speed_buildings_factor = -0.1
				training_time_factor = 0.2
			}
			cancel_if_invalid = no
		}	

		all_adults_serve = {
			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted all_adults_serve law"
			}
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.2 group = mobilization_laws }
				has_war = yes
				OR = {
					has_war_support > 0.7
					enemies_strength_ratio > 0.75
					surrender_progress > 0.13
				}
				OR = {
					has_idea = service_by_requirement
					has_idea = all_adults_serve
					has_idea = scraping_the_barrel
				}
				if = {
					limit = {
						OR = {
							tag = GER
							tag = DDR
							tag = WGR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}
			}

			ai_will_do = {
				base = 600
				modifier = {
					#Prio if low on manpower
					factor = 20
					manpower_per_military_factory <  600
				}
				modifier = {
					#Lower chance if has decent amount
					factor = 0.5
					manpower_per_military_factory >  1000
				}
				modifier = {
					#Block has plenty manpower
					factor = 0
					NOT = { tag = CHI }
					manpower_per_military_factory >  5000
				}

				modifier = { # china relies a lot on throwing bodies at the enemy
					factor = 3

					tag = CHI
					has_manpower < 200000
				}
				modifier = {
					is_subject = yes
					is_major = no
					has_war = no
					factor = 0
				}
				modifier = {
					factor = 10
					original_tag = SOV
					date > 1943.1.1
					has_manpower < 2000000	
				}
				modifier = {
					factor = 0
					has_war = yes
					enemies_strength_ratio < 0.5
					has_manpower > 0
				}
			}

			cost = 150
			removal_cost = -1
			level = 2

			modifier = {
				conscription = 0.2
				industrial_capacity_factory = -0.3
				industrial_capacity_dockyard = -0.3
				production_speed_buildings_factor = -0.3
				#XP from reinforcement penalty (small)
				training_time_factor = 0.3
			}
			cancel_if_invalid = no
		}

		scraping_the_barrel = {
			on_add = {
				log = "[GetDateText]/ [Root.GetName]: enacted scraping_the_barrel law"
			}
			available = {
				#has_manpower_for_recruit_change_to =  { value = 0.25 group = mobilization_laws }
				has_war = yes
				OR = {
					has_war_support > 0.85
					surrender_progress > 0.25
				}
				enemies_strength_ratio > 1
				#any_enemy_country = {
				#	strength_ratio = {
				#		tag = ROOT 
				#		ratio > 1
				#	}
				#}
				if = {
					limit = {
						OR = {
							tag = GER
							tag = DDR
							tag = WGR
						}
					}
					NOT = { has_idea = versailles_treaty }
				}
				if = { limit = { original_tag = JAP }
					always = no
				}
			}

			ai_will_do = {
				base = 800
				modifier = {
					#Prio if low on manpower
					factor = 20
					manpower_per_military_factory <  500
				}				
				modifier = {
					#Lower chance if has decent amount
					factor = 0.5
					manpower_per_military_factory >  1000
				}
				modifier = {
					#Block has plenty manpower
					factor = 0
					manpower_per_military_factory >  5000
				}
				modifier = {
					is_subject = yes
					is_major = no
					has_war = no
					factor = 0
				}
				modifier = {
					factor = 0
					has_war = yes
					enemies_strength_ratio < 0.5
					has_manpower > 0
				}
			}

			cost = 150
			removal_cost = -1
			level = 1
			
			modifier = {
				conscription = 0.25
				industrial_capacity_factory = -0.4
				industrial_capacity_dockyard = -0.4
				production_speed_buildings_factor = -0.4
				training_time_factor = 0.5
			}
			cancel_if_invalid = no
		}
	}
}
