
add_namespace = news

news_event = {
	id = news.1
	title = "marco polo"
	desc = news.1.d
	picture = GFX_news_event_001
	
	major = yes
	
	is_triggered_only = yes
	
	option = {
		name = news.1.a
		trigger = {
			NOT = {
				TAG = JAP
				TAG = CHI
			}
		}
	}
}

# Historical Paris World's Fair
news_event = {
	id = news.2
	title = news.2.t
	desc = news.2.desc
	picture = x

	is_triggered_only = yes
	# fire_only_once = yes
	
	trigger = {
		country_exists = GER
		country_exists = SOV
		country_exists = FRA

		GER = {
			has_country_flag = GER_hitler_in_power
			has_government = fascism
		}

		OR = {
			SOV = { has_government = communism }
			SOV = { has_government = authoritarian_socialism }
		}
		NOT = { 
			has_global_flag = GCW_ongoing_flag
			FRA = { has_war = yes }
		}
	}

	option = {
		name = news.2.a
		
	}

	option = {
		name = news.2.b
		
	}
}

news_event = {
	id = news.3
	title = news.3.t
	desc = news.3.desc
	picture = x

	is_triggered_only = yes
	# fire_only_once = yes
	
	trigger = {
		country_exists = SOV
		country_exists = FRA

		OR = {
			SOV = { has_government = communism }
			SOV = { has_government = authoritarian_socialism }
		}
		NOT = { 
			has_global_flag = GCW_ongoing_flag
			FRA = { has_war = yes }
		}
	}

	option = {
		name = news.3.a
		
	}

	option = {
		name = news.3.b
		
	}
}

# Yugoslav Coup
news_event = {
	id = news.4
	title = news.150.t
	desc = news.150.d
	picture = GFX_news_event_yugoslav_coup
	
	major = yes
	
	is_triggered_only = yes
	# fire_only_once = yes
	trigger = {
		NOT = { tag = YUG }
	}
	
	option = {
		name = "Oh"
		trigger = {
			NOT = {
				tag = GER
				is_ally_with = GER
				tag = ENG
			}
		}
	}

	option = {
		name = "Yes!"
		trigger = {
			tag = ENG
		}
	}

	option = {
		name = news.150.b
		trigger = {
			OR = {
				tag = GER
				is_ally_with = GER
			}
		}
	}
}